# results

run `python ../run_clustering.py` after placing CICIDS2017 csv files in `../data/`.

the script generates:
- feature_summary.csv
- attack_label_counts.csv
- pca_projection.png
- kmeans_metrics.csv
- cluster_assignments.csv

these files are the actual review-2 results once the pipeline has been executed on the project dataset.
